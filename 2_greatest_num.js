/*Take two id values from the user and print greatest among them*/
let p = prompt("Enter the first nummber:");
a = Number.parseInt(p);
let q = prompt("Enter the second nummber:");
b = Number.parseInt(q);
if (p > q) 
{
  console.log(" first number is greater than second number");
}
else 
{
  console.log(" first number is smaller than second number");
}